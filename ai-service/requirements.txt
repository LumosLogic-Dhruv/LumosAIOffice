# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# AI — Groq (primary, fast) + Gemini (fallback)
groq>=0.9.0
google-genai>=0.3.0

# HTTP client (for Convex API)
httpx>=0.27.0

# Validation & env
pydantic>=2.6.0
python-dotenv>=1.0.0

# File uploads
python-multipart>=0.0.9

# Cloudinary
cloudinary>=1.36.0

# PDF generation (run `playwright install chromium` after pip install)
playwright>=1.40.0
